package config

// Validate performs placeholder validation for NOORCHAIN configuration.
// In Phase 2 this always returns true with no error.
func Validate(cfg Config) (bool, error) {
	// Placeholder: always valid
	return true, nil
}
