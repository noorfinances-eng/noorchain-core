# NOORCHAIN Testnet 1.0 — Commandes pour lancer un nœud (plus tard)

Ce document sert de guide **pratique** pour le jour où NOORCHAIN sera lancé
en local (ou sur un serveur) avec le binaire `noord`.

⚠️ IMPORTANT  
Ces commandes ne doivent pas être exécutées maintenant.  
Elles sont prévues pour plus tard, quand :

- Go sera installé,
- le repo noorchain-core sera cloné en local,
- le binaire `noord` aura été compilé.

---

## 1. Compilation du binaire `noord` (plus tard)

Depuis le dossier du projet local :

```bash
cd noorchain-core

# (exemple générique, à adapter selon la structure finale)
make install
