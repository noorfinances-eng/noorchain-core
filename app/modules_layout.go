package app

// AppModules regroupe les modules de l'application NOORCHAIN.
//
// V1 minimal : on ne définit ici qu'une structure vide.
// On la remplira plus tard quand on câblera réellement les modules
// (auth, bank, staking, gov, evm, feemarket, noorsignal, etc.).
type AppModules struct {
	// Exemple pour plus tard :
	// Auth      interface{}
	// Bank      interface{}
	// Staking   interface{}
	// Gov       interface{}
	// Evm       interface{}
	// FeeMarket interface{}
	// NoorSignal interface{}
}
